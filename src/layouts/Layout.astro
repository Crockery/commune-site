---
import '@fontsource/poppins/400.css';
import '@fontsource/poppins/500.css';
import '@fontsource/poppins/600.css';

import Logo from './components/Logo.astro'

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Commune is a World of Warcraft guild." />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover"
    />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body>
		<header>
			<Logo />
			<h1>COMMUNE</h1>
			<Logo />
		</header>
		<nav>
			<a href="https://discord.gg/zqJpYKDj86" target="_blank">JOIN US</a>
			<a href="https://www.warcraftlogs.com/guild/us/wyrmrest-accord/commune" target="_blank">LOGS</a>
			<a href="https://worldofwarcraft.blizzard.com/en-us/guild/us/wyrmrest-accord/commune" target="_blank">PROFILE</a>
		</nav>
		<main>
			<div class="marquee">
				<ul class="marquee-content">
					<li>THE SWORD IS STILL IN THERE, IS SOMEONE GOING TO REMOVE IT? WHAT IF NO ONE DOES? DOES NO ONE REMEMBER THAT ITS THERE? WHAT IF NO ONE REMEMBERS THAT IM HERE, LIKE THAT SWORD? WHAT IF WE'RE ALL THE SWORD?</li>
				</ul>
				<ul class="marquee-content" aria-hidden="true">
					<li>THE SWORD IS STILL IN THERE, IS SOMEONE GOING TO REMOVE IT? WHAT IF NO ONE DOES? DOES NO ONE REMEMBER THAT ITS THERE? WHAT IF NO ONE REMEMBERS THAT IM HERE, LIKE THAT SWORD? WHAT IF WE'RE ALL THE SWORD?</li>
				</ul>
			</div>
			<slot />
		</main>
	</body>
</html>

<style is:global>
	*, *::before, *::after {
		box-sizing: border-box;
	}
	* {
		margin: 0;
	}
	body {
		line-height: 1.5;
		-webkit-font-smoothing: antialiased;
	}
	img, picture, video, canvas, svg {
		display: block;
		max-width: 100%;
	}
	input, button, textarea, select {
		font: inherit;
	}
	p, h1, h2, h3, h4, h5, h6 {
		overflow-wrap: break-word;
	}
	:root {
		--white: 255, 255, 255;
		--black: 0, 0, 0;
		--accent-light: 248, 230, 254;
		--accent: 241, 198, 255;
		--accent-dark: 191, 0, 254;
		--header-height: 282px;
		--nav-height: 62px;
		--marquee-height: 140px;
		--border-width-thick: 5px;
		--border-width: 3px;
	}
	h1 {
		font-size: 80px;
		text-decoration: underline;
		font-weight: 600;
		margin: 0;
    color: rgb(var(--accent-dark))
	}
	html {
		font-family: 'Poppins', sans-serif;
		background: rgb(var(--white));
	}
	header {
		width: 100%;
		height: var(--header-height);
		display: flex;
		align-items: center;
		justify-content: space-between;
		padding: 0px 60px;
	}
	li {
		list-style: none;
		margin: 0;
		padding: 0;
	}
	main {
		width: 100%;
		display: flex;
		flex-direction: column;
		align-items: flex-start;
		justify-content: flex-start;
	}

	@keyframes scroll {
		from {
			transform: translateX(0);
		}
		to {
			transform: translateX(calc(-100% - var(--gap)));
		}
	}

	nav {
		height: var(--nav-height);
		width: 100%;
		display: flex;
		align-items: center;
		justify-content: space-around;
		border-top: var(--border-width) solid rgb(var(--accent-dark));
		border-bottom: var(--border-width) solid rgb(var(--accent-dark));
	}

	nav a {
		font-size: 18px;
		font-weight: 600;
		text-decoration: none;
		color: rgb(var(--accent-dark));
	}

	nav a:hover {
		text-decoration: underline;
	}

	.marquee {
		--gap: 8px;
		position: relative;
		height: var(--marquee-height);
		align-items: center;
		display: flex;
		overflow: hidden;
		user-select: none;
		gap: var(--gap);
	}

	.marquee-content {
		flex-shrink: 0;
		display: flex;
		justify-content: space-around;
		gap: var(--gap);
		min-width: 100%;
		animation: scroll 30s linear infinite;
	}

	.marquee-content li {
		font-size: 60px;
		color: rgb(var(--accent-dark));
		font-weight: 400;
	}

	@media (prefers-reduced-motion: reduce) {
		.marquee-content {
			animation-play-state: paused !important;
		}
	}

  @media screen and (max-width: 1200px) {
		.marquee-content li {
			font-size: 48px;
		}
		:root {
			--header-height: 170px;
			--marquee-height: 100px;
		}
		h1 {
			font-size: 60px;
		}
		header {
			padding: 0px 50px
		}
	}
	@media screen and (max-width: 940px) {
		.marquee {
			border-top-width: 3px;
			border-bottom-width: 3px;

		}
		.marquee-content li {
			font-size: 36px;
		}
		:root {
			--header-height: 108px;
			--marquee-height: 80px;
			--nav-height: 54px;
			--border-width-thick: 4px;
			--border-width: 2px;
		}
		h1 {
			font-size: 40px;
		}
		header {
			padding: 0px 32px;
			position: sticky;
			top: 0;
			background-color: white;
			z-index: 4;
		}

		nav a {
			font-size: 16px;
		}
	}
	@media screen and (max-width: 500px) {
		.marquee-content li {
			font-size: 28px;
		}
		:root {
			--header-height: 84px;
			--marquee-height: 68px;
			--border-width-thick: 3px;
			--border-width: 1px;
			--nav-height: 40px;
		}
		h1 {
			font-size: 32px;
			text-align: center;
		}
		header {
			padding: 0px 24px
		}
		nav a {
			font-size: 14px;
		}
	}
</style>